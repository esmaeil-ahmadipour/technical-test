## Git History Note

This project was provided with an existing Git history.

The original commits belong to the provided repository history. For this technical task, I worked on top of that history and completed the required conflict resolution and merge between `dev` and `main`.

Task-related commit:

`d7ceaba` — `merge: resolve dev into main`
